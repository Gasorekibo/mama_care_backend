import { NoPasswordInterceptor } from './no-password.interceptor';

describe('NoPasswordInterceptor', () => {
  it('should be defined', () => {
    expect(new NoPasswordInterceptor()).toBeDefined();
  });
});
